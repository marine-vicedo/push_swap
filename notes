projet push swap

Pre requis :
afficher les instructions separes par /n
si pas d'ARG, on affiche rien (return O)
Cas d'erreur : si args ne sont pas des int, si int < INT MIN ou int > INT MAX, ou si duplicates >> afficher ERROR

Main :
fonctions write ou ft_printf
cas erreur


creer une liste chainee pour stack A
avec en parametres les args
